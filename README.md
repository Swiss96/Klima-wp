# KLIMA-WP Website

Cloudflare Workers + Static Assets Website für klima-wp.ch.

## Lokal
```bash
npm install
npm run dev
```

## Deployment
```bash
npm run deploy
```

Die statischen Dateien liegen in `public/`. Formulare/API werden über `src/index.js` verarbeitet.
